<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<form>
  <div class="form-group row">
    <%= f.label :first_name, class: "form-label col-sm-2" %>
    <%= f.text_field :first_name, class: "form-control col-sm-8", placeholder: "First Name" %>
  </div>

  <div class="form-group row">
    <%= f.label :last_name, class: "col-form-label col-sm-2" %>
    <%= f.text_field :last_name, class: "form-control col-sm-8", placeholder: "First Name" %>
  </div>

  <div class="actions">
    <%= f.submit class: "btn btn-primary"%>
  </div>
</form>

<% end %>

