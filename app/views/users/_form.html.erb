<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<form class="form-group">
  <div class="form-group row">
    <%= f.label :first_name, class: "col-sm-2 col-form-label" %>
    <%= f.text_field :first_name, class: "col-sm-8", placeholder: "First Name" %>
  </div>

  <div class="form-group row">
    <%= f.label :last_name, class: "col-sm-2 col-form-label" %>
    <%= f.text_field :last_name, class: "col-sm-8", placeholder: "First Name" %>
  </div>

  <div class="actions">
    <%= f.submit class: "btn btn-primary"%>
  </div>
<% end %>
</form>
